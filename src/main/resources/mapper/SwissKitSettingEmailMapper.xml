<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fan.summer.database.mapper.email.SwissKitSettingEmailMapper">

    <insert id="insert"
            parameterType="fan.summer.database.entity.email.SwissKitSettingEmailEntity"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO swiss_kit_setting_email (email, password, smtp_address, smtp_port, need_tls, need_ssl)
        VALUES (#{email}, #{password}, #{smtpAddress}, #{smtpPort}, #{needTLS}, #{needSSL})
    </insert>


</mapper>