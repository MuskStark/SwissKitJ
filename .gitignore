target/
!/mvn/wrapper/maven-wrapper.jar
/out/
!**/src/main/**/target/
!**/src/test/**/target/
.kotlin
# Local packaging output (web/desktop release artifacts staged by scripts/).
/release/

# Writable runtime state (may contain database credentials, encryption key material, and user data).
/config/
/database/
/logs/
/plugins/
/plugin-data/
/skills/
/runtime-files/
/FengYu/config/
/FengYu/database/
/FengYu/logs/
/FengYu/plugins/
/FengYu/plugin-data/
/FengYu/skills/
/FengYu/runtime-files/

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/
!frontend/build/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store
/FengYu-main/
/.swisskit/
**/.swisskit/
/.fengyu/
**/.fengyu/

### MCP ###
.mcp.json

### Plan / Work in progress ###
/plan
/testdata/

# Superpowers brainstorm artifacts
.superpowers/
/.zcode/

# Git worktrees (superpowers SDD)
.worktrees/

### Node / VitePress docs ###
node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/

### FengYu plugin build output (plugin-cli / official plugins) ###
# .fyp packages + transient staging/zip siblings produced by `fengyu plugin build`
**/dist-package/
**/.staging-*/
**/*.tmp-*
# Per-ui lockfile fingerprint written by ensureUiInstalled()
**/node_modules/.fengyu-lock-hash

### Local agent / IDE tool configs ###
.codex/

### Stray local dev / build artifacts (sources live under toolchain/) ###
# Tauri (Rust) shell was removed in favor of Electron (commit 8d5c7935);
# src-tauri/ is leftover and can reappear from an old toolchain on disk.
/desktop/src-tauri/
# electron-builder output (unpacked app, icon cache, builder-debug.yml).
# Regenerated by `npm run build`/release; never commit it.
/desktop/dist-electron/
# Local npm-install copies of the plugin toolchain packages. Canonical sources
# are toolchain/sdk-ts, toolchain/ui, toolchain/dev — never these root mirrors.
/plugin-sdk/
/plugin-ui/
/plugin-dev/
/plugin-cli/
.firecrawl/

# Ad-hoc screenshots from live GUI patrol sessions (kept on disk, never source).
/gui-test-screenshots/

# Python bytecode from local SDK test runs (__pycache__ is never source).
__pycache__/
*.pyc
